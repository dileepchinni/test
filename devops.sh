git hub is a centraized version control system
where as git hub is a local repositry system
git and git hub both are combined to get a repository to store our source code
and its a combination of git and github
it is a key to maintain our source code in a proper way
